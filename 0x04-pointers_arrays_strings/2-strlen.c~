#include "holberton.h"

/**
 * _strlen - return the length of a string
 *
 * @s: string whose length to return
 *
 * Return: length of @s
 */
int _strlen(char *s)
{
	int n;

	while (*s != '\0')
	{
		++n;
		*(*s) += 1;
	}
	return (n);
}

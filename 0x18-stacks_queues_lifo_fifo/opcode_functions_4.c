#include "monty.h"

/**
 * rotl_op - rotate the stack left
 *
 * @head: top of stack
 * @line_number: line number of opcode
 *
 * Return: void
 */
void rotl_op(stack_t **head, unsigned int line_number)
{
	(void) head;
	(void) line_number;
}

/**
 * rotr_op - rotate the stack right
 *
 * @head: top of stack
 * @line_number: line number of opcode
 *
 * Return: void
 */
void rotr_op(stack_t **head, unsigned int line_number)
{
	(void) head;
	(void) line_number;
}

/**
 * stack_op - change the format of the data to stack
 *
 * @head: top of stack
 * @line_number: line number of opcode
 *
 * Return: void
 */
void stack_op(stack_t **head, unsigned int line_number)
{
	(void) head;
	(void) line_number;
}

/**
 * queue_op - change the format of the data to queue
 *
 * @head: top of stack
 * @line_number: line number of opcode
 *
 * Return: void
 */
void queue_op(stack_t **head, unsigned int line_number)
{
	(void) head;
	(void) line_number;
}

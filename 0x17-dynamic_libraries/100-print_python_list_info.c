#include "holberton.h"
#include <stdio.h>

void print_python_list_info(PyObject *p)
{
	int i, size, allocated;

	printf("[*] Size of the Python List = %d\n", size);
	printf("[*] Allocated = %d\n", allocated);
	for (i = 0; something != NULL; i++)
		printf("Element %d: %s\n", element_num, type);
}
